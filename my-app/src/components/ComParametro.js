
import React from 'react'

const comparando = (props)=>{
   

const {titulo,idade}= props

    return (
        <div>
            <h1>{titulo}</h1>
            <h1>{idade}</h1>
        </div>
    )
}

export default comparando